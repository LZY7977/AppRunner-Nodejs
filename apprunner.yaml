version: 1.0
runtime: nodejs18
build:
  env:
    - name: test
      value: env-test
  commands:
    pre-build:
      - echo pre-build
      - echo $test
    build:
      - echo build
      - echo $test
    post-build:
      - echo post-build
      - echo $test
run:
  pre-run:
    - echo getting IDMDSv2 Token
  command: node app.js
