version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - echo pre-build
      - whoami
    build:
      - echo build
      - whoami
    post-build:
      - echo post-build
      - whoami
run:
  pre-run:
    - echo pre-run
    - whoami
  runtime-version: 18.18.0
  command: node app.js
